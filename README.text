How to make a HTML client with reqwest!

Before we do anything, we are going to take a look at reqwest. First, you need to go to "Cargo.toml".
If you need a cookie store and JSON, put

    reqwest = { version = "0.10.4", features = ["cookies","json"]}.
    tokio = { version = "0.2", features = ["full"] }

